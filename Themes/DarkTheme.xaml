<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Modern Dark Theme -->
    <Color x:Key="AccentColor">#60CDFF</Color>
    <SolidColorBrush x:Key="AccentBrush" Color="#60CDFF" />
    <SolidColorBrush x:Key="AccentHoverBrush" Color="#80D4FF" />
    <SolidColorBrush x:Key="AccentPressedBrush" Color="#40B8F0" />

    <SolidColorBrush x:Key="BackgroundBrush" Color="#1E1E1E" />
    <SolidColorBrush x:Key="ForegroundBrush" Color="#D4D4D4" />
    <SolidColorBrush x:Key="SurfaceBrush" Color="#252525" />
    <SolidColorBrush x:Key="PanelHeaderBrush" Color="#2D2D2D" />
    <SolidColorBrush x:Key="HeaderForegroundBrush" Color="#E0E0E0" />
    <SolidColorBrush x:Key="BorderBrush" Color="#3E3E3E" />
    <SolidColorBrush x:Key="ToolBarBackground" Color="#2D2D2D" />
    <SolidColorBrush x:Key="ToolBarBorder" Color="#3E3E3E" />
    <SolidColorBrush x:Key="ToolBarSeparatorBrush" Color="#444444" />
    <SolidColorBrush x:Key="SplitterBrush" Color="#3E3E3E" />
    <SolidColorBrush x:Key="SplitterHoverBrush" Color="#60CDFF" />
    <SolidColorBrush x:Key="StatusBarBackground" Color="#2D2D2D" />
    <SolidColorBrush x:Key="StatusBarForeground" Color="#999999" />
    <SolidColorBrush x:Key="EditorBackground" Color="#1E1E1E" />
    <SolidColorBrush x:Key="EditorForeground" Color="#D4D4D4" />
    <SolidColorBrush x:Key="EditorLineNumBrush" Color="#666666" />
    <SolidColorBrush x:Key="FormatToolbarBackground" Color="#2A2A2A" />
    <SolidColorBrush x:Key="FormatToolbarBorder" Color="#3E3E3E" />

    <!-- Menu colors -->
    <SolidColorBrush x:Key="MenuBackground" Color="#2D2D2D" />
    <SolidColorBrush x:Key="MenuPopupBackground" Color="#252525" />
    <SolidColorBrush x:Key="MenuPopupBorder" Color="#3A3A3A" />
    <SolidColorBrush x:Key="MenuItemHover" Color="#3A3A3A" />
    <SolidColorBrush x:Key="MenuItemPressed" Color="#4A4A4A" />
    <SolidColorBrush x:Key="MenuSeparatorBrush" Color="#444444" />

    <!-- Flat button style -->
    <Style x:Key="ModernButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}"
                            CornerRadius="4" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#3A3A3A" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#4A4A4A" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Format toolbar button (compact, icon-only) -->
    <Style x:Key="FormatButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
        <Setter Property="Width" Value="32" />
        <Setter Property="Height" Value="28" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <!-- Toolbar separator style -->
    <Style x:Key="ToolbarSeparator" TargetType="Separator">
        <Setter Property="Width" Value="1" />
        <Setter Property="Height" Value="20" />
        <Setter Property="Background" Value="{DynamicResource ToolBarSeparatorBrush}" />
        <Setter Property="Margin" Value="4,0" />
    </Style>

    <!-- ========== MENU STYLES ========== -->
    <Style TargetType="Menu">
        <Setter Property="Background" Value="{DynamicResource MenuBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
        <Setter Property="Grid.IsSharedSizeScope" Value="True" />
    </Style>

    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{DynamicResource MenuPopupBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource MenuPopupBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="2" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.25" />
                        </Border.Effect>
                        <StackPanel IsItemsHost="True"
                                    KeyboardNavigation.DirectionalNavigation="Cycle" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Padding" Value="10,5" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="RootBorder"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            Padding="{TemplateBinding Padding}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Icon" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Shortcut" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!-- Icon area -->
                            <ContentPresenter x:Name="Icon"
                                              Grid.Column="0"
                                              ContentSource="Icon"
                                              Margin="0,0,8,0"
                                              VerticalAlignment="Center"
                                              Width="16" Height="16" />

                            <!-- Header text -->
                            <ContentPresenter x:Name="HeaderHost"
                                              Grid.Column="1"
                                              ContentSource="Header"
                                              RecognizesAccessKey="True"
                                              VerticalAlignment="Center" />

                            <!-- Shortcut text -->
                            <TextBlock x:Name="InputGesture"
                                       Grid.Column="2"
                                       Text="{TemplateBinding InputGestureText}"
                                       Margin="30,0,4,0"
                                       Foreground="{DynamicResource EditorLineNumBrush}"
                                       VerticalAlignment="Center" />

                            <!-- Submenu arrow -->
                            <Path x:Name="Arrow"
                                  Grid.Column="3"
                                  Data="M 0 0 L 4 4 L 0 8 Z"
                                  Fill="{TemplateBinding Foreground}"
                                  Margin="6,0,2,0"
                                  VerticalAlignment="Center"
                                  Visibility="Collapsed" />

                            <!-- Popup for submenus -->
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   Placement="Right"
                                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   PopupAnimation="{DynamicResource {x:Static SystemParameters.MenuPopupAnimationKey}}">
                                <Border x:Name="PopupBorder"
                                        Background="{DynamicResource MenuPopupBackground}"
                                        BorderBrush="{DynamicResource MenuPopupBorder}"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Padding="2">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.25" />
                                    </Border.Effect>
                                    <StackPanel IsItemsHost="True"
                                                KeyboardNavigation.DirectionalNavigation="Cycle" />
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <!-- Top-level menu items -->
                        <Trigger Property="Role" Value="TopLevelHeader">
                            <Setter TargetName="PART_Popup" Property="Placement" Value="Bottom" />
                            <Setter TargetName="Arrow" Property="Visibility" Value="Collapsed" />
                            <Setter Property="Padding" Value="6,3" />
                        </Trigger>
                        <Trigger Property="Role" Value="TopLevelItem">
                            <Setter TargetName="PART_Popup" Property="Placement" Value="Bottom" />
                            <Setter TargetName="Arrow" Property="Visibility" Value="Collapsed" />
                            <Setter Property="Padding" Value="6,3" />
                        </Trigger>
                        <!-- Submenu items show arrow -->
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="Arrow" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <!-- Hover -->
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource MenuItemHover}" />
                        </Trigger>
                        <!-- Disabled -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="#666666" />
                        </Trigger>
                        <!-- Checked items -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource AccentBrush}" />
                            <Setter Property="Foreground" Value="#FFFFFF" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Separator inside menus -->
    <Style TargetType="{x:Type Separator}" x:Key="{x:Static MenuItem.SeparatorStyleKey}">
        <Setter Property="Height" Value="1" />
        <Setter Property="Background" Value="{DynamicResource MenuSeparatorBrush}" />
        <Setter Property="Margin" Value="6,4" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border Background="{TemplateBinding Background}"
                            Height="{TemplateBinding Height}"
                            Margin="{TemplateBinding Margin}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Window -->
    <Style TargetType="Window">
        <Setter Property="Background" Value="{DynamicResource BackgroundBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI, sans-serif" />
    </Style>

    <!-- Top toolbar -->
    <Style TargetType="ToolBar">
        <Setter Property="Background" Value="{DynamicResource ToolBarBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ToolBarBorder}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <!-- Editor textbox -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource EditorBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource EditorForeground}" />
        <Setter Property="CaretBrush" Value="{DynamicResource EditorForeground}" />
    </Style>

    <!-- ToolTip -->
    <Style TargetType="ToolTip">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolTip">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- StatusBar -->
    <Style TargetType="StatusBar">
        <Setter Property="Background" Value="{DynamicResource StatusBarBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource StatusBarForeground}" />
        <Setter Property="BorderThickness" Value="0,1,0,0" />
        <Setter Property="BorderBrush" Value="{DynamicResource ToolBarBorder}" />
    </Style>

    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
    </Style>
</ResourceDictionary>